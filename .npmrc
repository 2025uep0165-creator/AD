# The audit tooling (scripts/audit.mjs) needs Playwright's API, not its browsers.
# Without this, `npm install` pulls ~400MB of Chromium — including on Vercel.
# Run `npx playwright install chromium` if you want to run the audit locally.
playwright_skip_browser_download=1
